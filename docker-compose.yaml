version: '3.8'

services:
  food-ordering:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - LOG_LEVEL=info
    # Mounting requirements allows local coupon files to be swapped without rebuilding the image
    volumes:
      - ./requirements:/app/requirements
    restart: unless-stopped
